{
  "name": "template-vercel-express-dynamodb",
  "version": "1.0.0",
  "description": "Plantilla productiva: Express + frontend estático + DynamoDB (AWS) desplegada gratis en Vercel, con showcase de arquitectura integrado.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test tests/app.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1087.0",
    "@aws-sdk/lib-dynamodb": "^3.1087.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "engines": {
    "node": ">=20"
  }
}
