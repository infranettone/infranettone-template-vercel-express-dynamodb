{
  "name": "vedtemplate",
  "version": "1.0.0",
  "description": "Production-ready template: Express + static frontend + DynamoDB (AWS) deployed free on Vercel, with a built-in architecture showcase and traffic-monitoring dashboard.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test tests/app.test.js tests/traffic.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1087.0",
    "@aws-sdk/lib-dynamodb": "^3.1087.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "engines": {
    "node": ">=20"
  }
}
